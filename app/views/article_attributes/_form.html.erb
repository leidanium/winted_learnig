<%= form_with(model: article_attribute, local: true) do |form| %>
  <% if article_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article_attribute.errors.count, "error") %> prohibited this article_attribute from being saved:</h2>

      <ul>
        <% article_attribute.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
