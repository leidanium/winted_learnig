<%= form_with(model: characteristic, local: true) do |form| %>
  <% if characteristic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(characteristic.errors.count, "error") %> prohibited this characteristic from being saved:</h2>

      <ul>
        <% characteristic.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :v_type %>
    <%= form.number_field :v_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
