<%= form_with(model: sold, local: true) do |form| %>
  <% if sold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sold.errors.count, "error") %> prohibited this sold from being saved:</h2>

      <ul>
        <% sold.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :article_id %>
    <%= form.text_field :article_id %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
